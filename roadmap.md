# Roadmap

- [x] Replace `_shared/buildReportHtml.ts` with the multilingual version.
- [ ] Run `deno check` on the replaced file.
- [ ] Generate test reports in `en` and `hi` via admin-create-free-report and confirm PDFs render as before.
